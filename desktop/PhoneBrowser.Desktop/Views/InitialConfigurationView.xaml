<UserControl x:Class="PhoneBrowser.Desktop.Views.InitialConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhoneBrowser.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Skonfigurujmy aplikację"
                FontSize="25" FontWeight="SemiBold"
                HorizontalAlignment="Center"
                Margin="0,0,0,16"
                Grid.Row="0"/>

        <TextBlock Text="Domyślne ustawienia można zmienić później w dowolnym momencie."
                FontSize="17" FontWeight="SemiBold"
                HorizontalAlignment="Center"
                Margin="0,0,0,16"
                Grid.Row="1"/>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Grid.Row="2" Text="Nazwa tego komputera"/>
            <TextBox Grid.Row="2" Name="inputBox" Width="200" Height="30" Text="{Binding DeviceName}" />
        </StackPanel>
        
        <Button Grid.Row="3" Content="Zacznij korzystać" Command="{Binding NextCommand}" />
    </Grid>
</UserControl>

